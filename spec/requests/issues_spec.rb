require 'spec_helper'

describe "Issues" do
  describe "GET /issues" do
    pending "works! (now write some real specs)" do
      get project_issues_path
      response.status.should be(200)
    end
  end
end
